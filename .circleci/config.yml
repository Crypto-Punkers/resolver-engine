version: 2
jobs:
  build:
    docker:
        - image: circleci/node:9
    steps:
        - checkout
        - run:
            name: Install modules
            command: yarn --frozen-lockfile install
        - run:
            name: Build
            command: yarn build
        - run:
            name: Tests
            command: yarn test
