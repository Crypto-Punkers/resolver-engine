{
  "private": true,
  "workspaces": [
    "packages/*",
    "tests"
  ],
  "scripts": {
    "build": "tsc -b tsproject.json",
    "watch": "tsc -b --watch",
    "clean": "wsrun --exclude-missing clean",
    "test": "yarn workspace tests test"
  },
  "devDependencies": {
    "wsrun": "^3.6.2"
  }
}
