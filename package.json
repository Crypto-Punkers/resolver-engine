{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b tsproject.json",
    "watch": "tsc -b --watch",
    "clean": "wsrun clean",
    "test": "wsrun --collect-logs --report test"
  },
  "devDependencies": {
    "typescript": "^3.2.4",
    "wsrun": "^3.6.2"
  }
}
